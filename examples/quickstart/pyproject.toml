[project]
name = "semflow-quickstart"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = ["typer>=0.12"]

[project.scripts]
example = "example.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/example"]
