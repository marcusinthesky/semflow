# semflow

`semflow` gives DVC a semantic fingerprint for the Python code reachable from
each Typer command. This can avoid rerunning a stage when unrelated code or
formatting changes.

Start with the [Typer and DVC quickstart](getting-started.md), then read the
[concepts](concepts.md) and [Python API reference](reference.md).
